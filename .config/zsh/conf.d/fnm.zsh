# ╭───────────────────────────╮
# │ Fast Node Manager         │
# ╰───────────────────────────╯
# Note: fnm env init happens in .zshenv for all shells
# Add completions for interactive shell
eval "$(fnm completions --shell zsh)"
