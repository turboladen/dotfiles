local nightfox = require("nightfox")

nightfox.setup({
    options = {
        dim_inactive = true,
        inverse = {
            match_paren = true,
            visual = true,
            search = true,
        },
    },
})
