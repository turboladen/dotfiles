# This is `bat`s configuration file. Each line either contains a comment or
# a command-line option that you want to pass to `bat` by default. You can
# run `bat --help` to get a list of all possible configuration options.

# Specify desired highlighting theme (e.g. "TwoDark"). Run `bat --list-themes`
# for a list of all available themes
--theme="Visual Studio Dark+"

# Enable this to use italic text on the terminal. This is not supported on all
# terminal emulators (like tmux, by default):
--italic-text=always

# Show line numbers, Git modifications, and file header
--style=numbers,changes,header

# Uncomment the following line to disable automatic paging:
#--paging=never

# Better mouse support for your terminal setup
--pager="less --RAW-CONTROL-CHARS --quit-if-one-screen --mouse"

# Syntax mappings: map a certain filename pattern to a language.
# Based on your development environment:

# Fish shell configurations
--map-syntax "*.fish:Fish"
--map-syntax "config.fish:Fish"

# Configuration files
--map-syntax "*.toml:TOML"
--map-syntax "starship.toml:TOML"
--map-syntax "topgrade.toml:TOML"

# Git and development tools
--map-syntax ".gitignore:Git Ignore"
--map-syntax ".gitconfig:Git Config"
--map-syntax ".gitmodules:Git Config"

# Lua (for Neovim config)
--map-syntax "*.lua:Lua"

# RON (Rusty Object Notation) for GitUI
--map-syntax "*.ron:Rust"

# Docker and container files
--map-syntax "Dockerfile*:Dockerfile"
--map-syntax "docker-compose*.yml:YAML"
--map-syntax "docker-compose*.yaml:YAML"

# Common config files without extensions
--map-syntax "Brewfile:Ruby"
--map-syntax "Gemfile:Ruby"
--map-syntax "Rakefile:Ruby"
--map-syntax "Procfile:Bash"
